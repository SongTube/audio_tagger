## 0.0.2

* Android: added a `namespace`, dropped the AndroidManifest `package`
  attribute, raised compileSdk to 36 / Java to 17 / minSdk to 24, and removed
  the stale Android Gradle Plugin classpath, for AGP 8 support.
* Android: moved the JitPack repository into the `rootProject.allprojects`
  block so consuming apps can resolve jaudiotagger.

## 0.0.1

* TODO: Describe initial release.
